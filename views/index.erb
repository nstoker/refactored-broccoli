<!--
  this is a ERB tag, Ruby's equivilant to mushtash
{{ }}
<%=  %> # will evaluate and output the result
<% %> # no output but will evaluate
<#% %> # comment out ERB tag which will not evaluate

-->

<!--
  Add javascript if you're more comfortable with that and use JSON endpoints to get the data you need.
  This starter file shows how you can have server side render Ruby into HTML for WEB 2.0 style applications
-->
<html>
  <head>
  </head>
  <body>
    <h1>Jeopardy!</h1>
    <form action="/answers">
      <% @csv.each.with_index do |row,index| %>
        <h2>Question <%= index+1%></h2>
        <p>For <strong><%= row[2] %></strong> <%= row[3] %></p>
        <input type = "text" name="ua[<%= index %>]">
        <!-- Normally I'd extract the answers and value would be obtained using the id number of
             the database record, but as we don't have that here... -->
        <input type = "hidden" name="aa[<%= index %>]" value="<%= row[4] %>">
        <input type = "hidden" name="va[<%= index %>]" value="<%= row[2] %>">
        <br/>
      <% end %>
      <br/>
      <h2>Submit your answers</h2>
      <input type = "submit" value="If you dare...">
    </form>
  </body>
</html>
